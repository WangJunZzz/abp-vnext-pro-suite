namespace {{ context.Project.NameSpace }}.{{ context.EntityModel.Code }};

public static class EfCore{{aggregateCode}}QueryableExtension
{
     public static IQueryable<{{aggregateCode}}> IncludeDetails(this IQueryable<{{aggregateCode}}> queryable, bool include = true)
     {
         if (!include) return queryable;
         return queryable
                  .Include(x => x.ApproveSupplierInfos)
                  .Include(x => x.ApproveLookUserInfos)
                  .Include(x => x.ApproveOperatorUserInfos)
                  .Include(x => x.WorkflowRequestLogs);
     }
}
